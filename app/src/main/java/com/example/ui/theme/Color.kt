package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Vibrant Orange & Crisp White Palette
val OrangePrimary = Color(0xFFFF6F00)       // Bright Orange Primary
val OrangePrimaryDark = Color(0xFFE65100)   // Deep burnt orange accent
val OrangeOnPrimary = Color(0xFFFFFFFF)     // Pure White text on orange
val OrangeContainer = Color(0xFFFFF3E0)     // Soft light orange container
val OrangeOnContainer = Color(0xFFBF360C)   // Deep orange text on soft container

val OrangeSecondary = Color(0xFFFF8F00)     // Vibrant secondary orange
val OrangeOnSecondary = Color(0xFFFFFFFF)   // White on secondary

val WhiteBackground = Color(0xFFFFFFFF)     // Clean white background
val WhiteSurface = Color(0xFFFFFFFF)        // Pure white card surfaces
val OrangeSurfaceVariant = Color(0xFFFFF8F2) // Very light orange tint for cards/variants
val OrangeBorder = Color(0xFFFFE0B2)        // Soft light orange borders

val TextPrimary = Color(0xFF21130B)         // Very dark charcoal for readable text
val TextMuted = Color(0xFF6E5D53)           // Soft dark brown/charcoal for secondary text
val TextOutline = Color(0xFFFFCC80)         // Border outlines

// Status Colors
val StatusExpiredRed = Color(0xFFD32F2F)
val StatusExpiredBg = Color(0xFFFFEBEE)

val StatusExpiringAmber = Color(0xFFF57C00)
val StatusExpiringBg = Color(0xFFFFF3E0)

val StatusValidGreen = Color(0xFF2E7D32)
val StatusValidBg = Color(0xFFE8F5E9)
